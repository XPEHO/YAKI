/**
 * Enum to determine which statistic type to show.
 * Used in StatisticView
 */
export enum STATISTICTYPE {
  basic = "basic",
  global = "global",
  declarations = "declarations",
  perTeam = "per-team",
  perTeammate = "per-teammate",
  perWeekday = "per-weekday",
}
